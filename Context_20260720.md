# CONTEXT.md — nounoulink · Cerveau Externe

> Dernière mise à jour : 20 juillet 2026  
> URL production : https://pajemploi-facile.org  
> Repo : github.com/vicepreze/nannypay-front

---

## 1. Stack Technique

| Couche | Outil | Version |
|--------|-------|---------|
| Framework | Next.js App Router | 14.2.35 |
| Langage | TypeScript strict | — |
| Style | Tailwind CSS + CSS vars custom | — |
| ORM | Prisma | 6.x |
| Base de données | PostgreSQL (Neon serverless) | — |
| Auth | Clerk | **v5.x** ⚠️ (v7 incompatible Next 14) |
| Hébergement | Vercel | — |
| Tests | Vitest | 92 tests |
| Package manager | npm | — |

### Variables d'environnement Vercel (obligatoires)
```
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY   # inlinée au BUILD TIME
CLERK_SECRET_KEY                    # runtime (middleware + API routes)
CLERK_WEBHOOK_SECRET                # validation webhook Svix
DATABASE_URL                        # Neon PostgreSQL
```

### Règles Clerk v5 (Next 14)
- Middleware : `auth().protect()` (PAS `await auth.protect()` qui est v7)
- Côté serveur : `const { userId } = await auth()` depuis `@clerk/nextjs/server`
- Côté client : `useAuth()`, `useClerk()` depuis `@clerk/nextjs`
- `User.id` en DB = Clerk user ID (`user_xxxx`) — sync via webhook `/api/webhooks/clerk`

---

## 2. Architecture du Projet

```
app/
├── layout.tsx                  # Root layout → <Providers> (ClerkProvider)
├── providers.tsx               # ClerkProvider avec garde si clé absente
├── page.tsx                    # Landing (LandingPage component)
├── sign-in/[[...sign-in]]/     # Page Clerk sign-in
├── sign-up/[[...sign-up]]/     # Page Clerk sign-up
├── rejoindre/                  # Invitation famille B
├── dashboard/                  # Dashboard post-login
├── gardes/[id]/
│   ├── settings/               # Config d'une garde existante
│   └── mois/[annee]/[mois]/    # Vue mensuelle (layout 2 colonnes depuis juin 2026)
└── nouvelle-garde/             # Wizard création (3 étapes)
    ├── layout.tsx              # Auth guard + TabsNav
    ├── acteurs/                # Étape 1 : nb enfants par famille
    ├── planning/               # Étape 2 : horaires hebdo
    └── paie/                   # Étape 3 : taux, indemnités, RAC

lib/
├── calcul.ts                   # CŒUR MÉTIER — toutes les formules
├── calcul.test.ts              # 92 tests Vitest
└── prisma.ts                   # Singleton Prisma client

prisma/
└── schema.prisma               # Modèles : User, Garde, Nounou, Famille,
                                #           Enfant, Modele, Mois
```

### SessionStorage — Wizard nouvelle garde
```
ng_acteurs   → { nbEnfantsA, nbEnfantsB, enfants[{prenom, fam}] }
ng_planning  → { planning, hNormalesSemaine, hSup25Semaine, hSup50Semaine }
ng_paie      → { repartitionA, racOptionActive, taux, navigo, indemKm,
                  indemEntretien, repartitionIndemA, aidesA, aidesB }
```

---

## 3. La Bible Métier — Règles de Calcul

### 3.1 Structure d'une Garde Partagée
- **1 nounou** pour **2 familles** (A et B)
- **2 à 3 enfants** au total, max 2 par famille, min 1 par famille
- Les deux familles sont co-employeurs : chacune paie une part du salaire net + ses charges

### 3.2 Calcul du Salaire Net Mensuel
```
hNorm  = min(hHebdo, 40)
hSup25 = min(max(0, hHebdo - 40), 8)   → +25%  (40h à 48h/sem)
hSup50 = max(0, hHebdo - 48)            → +50%  (>48h/sem)

salNetMens = (hNorm × 52/12 × taux
            + hSup25 × 52/12 × taux × 1.25
            + hSup50 × 52/12 × taux × 1.50) × ratioPresence
```
**ratioPresence** = (joursOuvrables - joursMaladie) / joursOuvrables

### 3.3 Répartition entre Familles
- `repartitionA` (0 → 1) : part du salaire net payée par Famille A
- `repartitionB` = `1 - repartitionA`
- **Calcul mode B** (`calcBModeRepartition`) : ratio proportionnel aux heures de présence de chaque enfant

### 3.4 Indemnités (indépendantes du ratio salarial)
- `repartitionIndemA` (défaut 0.5) : part des indemnités pour Famille A, **indépendante** de `repartitionA`
- S'applique à : **Navigo + Frais km + Frais d'entretien**
- Frais d'entretien = `joursEntretienBase × tauxPresenceJour × indemEntretien × indemRatio`
- ⚠️ PAS de multiplication par `qp` (ratio salarial) — bug corrigé mai 2026

### 3.5 Reste à Charge (RAC) — Mode Magique vs Expert

#### Mode Magique (défaut)
Le système calcule automatiquement le ratio optimal pour équilibrer le RAC entre familles.

**CMG CAF 2025 (barème Pajemploi)**
| Revenus fiscaux | 1 enfant | 2 enfants |
|----------------|----------|-----------|
| ≤ 31 000 € | 1 075 €/mois | 1 612 €/mois |
| ≤ 57 000 € | 755 €/mois | 1 132 €/mois |
| > 57 000 € | 570 €/mois | 855 €/mois |

**Crédit d'impôt (CI) — plafond annuel**
- 1 enfant : 6 750 €/an → 562,50 €/mois
- 2+ enfants : 7 500 €/an → 625,00 €/mois
- Taux : 50% des dépenses nettes d'emploi

**Algorithme `calcEquitableRatioIteratif`** : balayage de 1% à 99% (990 itérations) pour trouver le `repartitionA` qui minimise l'écart RAC entre A et B.

#### Mode Expert (déverrouillé manuellement)
L'utilisateur saisit manuellement les 5 aides par famille :
`cmgCotisations`, `cmgRemuneration`, `abattementCharges`, `aideVille`, `creditImpot`

Pré-rempli depuis les estimations du Mode Magique. Reset disponible.

### 3.6 Congés & Absences
- `congesJson` sur le modèle Garde : config des congés payés
- `evenementsJson` sur Mois : liste des événements (CP, maladie nounou)
- Les congés payés ne réduisent PAS le salaire mensuel (provision mensuelle)
- La maladie de la nounou réduit le `ratioPresence`

---

## 4. Principes UX/UI

### 4.1 Progressive Disclosure (Divulgation Progressive)
**Principe cardinal** : ne montrer la complexité que quand l'utilisateur la demande.

- Étape 1 (acteurs) : uniquement les compteurs d'enfants — pas de noms
- Mode RAC : "Mode Magique" par défaut, bouton "⚙️ Ajuster manuellement" pour Mode Expert
- Settings : onglets séparés par domaine (acteurs, planning, paie, **personnaliser**)

### 4.2 Smart Defaults
Tout champ a une valeur par défaut sensée — l'utilisateur ne DOIT rien saisir pour avancer :
- Prénoms enfants : **Simone, Giselle, Suzanne** (charmants, mémorables, modifiables)
- Taux horaire : 11 €/h net
- Navigo : 90,80 €/mois
- Frais entretien : 6 €/jour
- Répartition indemnités : 50/50
- Répartition RAC : calculée automatiquement (Mode Magique)

### 4.3 Inputs Numériques — Pattern "Raw State"
Pour éviter les inputs incontrôlables (bug où taper "66" était impossible) :
```tsx
const [raw, setRaw]         = useState(() => String(Math.round(value * 100)));
const [focused, setFocused] = useState(false);
// Sync depuis parent seulement si pas focused
useEffect(() => {
  if (!focused) setRaw(String(Math.round(value * 100)));
}, [value, focused]);
```

### 4.4 Design Tokens (CSS vars)
```css
--sage        /* vert principal CTA */
--sage-light  /* fond vert pâle */
--ink         /* texte principal */
--dust        /* texte secondaire */
--paper       /* fond gris clair */
--line        /* bordures */
--red         /* erreurs */
--radius      /* border-radius global */
```

### 4.5 Grammaire Couleur — Vue Mensuelle (juin 2026)

Implémentée dans `components/CalendrierMoisView.tsx`. Chaque cellule du calendrier a un `DayType` :

```ts
type DayType = 'worked' | 'cp' | 'sick' | 'holiday' | 'off';
```

**Priorité de détection** (ordre décroissant) :
1. Jour hors du mois courant → `off`
2. Jour férié français → `holiday`
3. Événement `conge_paye` → `cp`
4. Événement `maladie_nounou` → `sick`
5. Sinon → `worked`

**Styles cellule (CELL_STYLE)** :
| DayType | Background | Border |
|---------|-----------|--------|
| worked | `#EAF3DE` | `#C0DD97` |
| cp | `#FCE8F3` | `#F4C0D1` |
| sick | `#FCEBEB` | `#F09595` |
| holiday | `#F1EFE8` | `#D3D1C7` |
| off | `var(--paper)` + opacity 0.35 | `var(--line)` |

**Couleur numéro de jour (DAY_NUM_COLOR)** :
`worked` → `#27500A` / `cp` → `#72243E` / `sick` → `#791F1F` / `holiday` → `#444441` / `off` → `var(--dust)`

**Tags dans la cellule** :
| DayType | Label | Style |
|---------|-------|-------|
| worked | "travaillé" | transparent bg, bordure `#3B6D11`, texte `#3B6D11` |
| cp | "congé payé" | bg `#F4C0D1`, texte `#72243E` |
| sick | "maladie" | bg `#FAEEDA`, texte `#633806` |
| holiday | "férié" | bg `#D3D1C7`, texte `#444441` |
| off | _(aucun tag)_ | — |

**Jours fériés français** : auto-détectés via algorithme de Pâques grégorien + liste fixe des 11 fériés nationaux. Fonction `frenchHolidays(year)` dans `CalendrierMoisView.tsx`.

**Heures/jour** : sur les jours `worked`, affichage de `heuresParJour` (prop calculée dans la page parente depuis `modele.hNormalesSemaine / joursActifsParSemaine`). Si `hasOvertime`, badge `+` amber.

---

## 5. État des Lieux — Juin 2026

### ✅ Terminé et en production
- Auth Clerk v5 (Google OAuth) — migration complète depuis NextAuth
- Webhook Clerk → DB (sync User)
- Wizard nouvelle garde simplifié (3 étapes : acteurs compteurs / planning / paie)
- Calcul Pajemploi complet (`lib/calcul.ts`) — 92 tests verts
- Fix entretien (suppression du bug `× qp`)
- Répartition indemnités configurable (`repartitionIndemA`) — indépendante du ratio salarial
- Mode Magique RAC (calcul itératif) + Mode Expert (aides manuelles)
- Landing page, Blog, pages sign-in/sign-up Clerk
- Invitation famille B via token
- **Vue mensuelle refactorisée** (PR #19, 26 juin 2026) : sidebar gauche supprimée, layout 2 colonnes `[1fr 280px]`, bannière stats supprimée, carte Validation supprimée, lignes RAC supprimées du panneau droit
- **Grammaire couleur calendrier** (Niveau 1 + 2) : cellules colorées par `DayType`, tags dans chaque cellule, détection automatique des fériés français
- **Absences famille A/B** : événements `absence_famille_a`/`absence_famille_b`, sans impact financier, cumulables avec n'importe quel autre événement (maladie/CP restent mutuellement exclusifs entre eux)
- **Bloc "Prévu → Réel"** dans la sidebar : comparaison théorique/réel par famille (salaire + entretien), waterfall des événements, n'apparaît qu'en cas d'écart réel — remplace l'ancien `DetailedCalcTable`
- **Notes maladie CCN IDCC 3239** : démarche Cerfa S3201 (vue familles, arrêt ≥ 4j ouvrés) + démarche CPAM/IRCEM (vue nounou)
- **Hook pre-commit ESLint** : `npx next lint` s'exécute automatiquement avant chaque `git commit` via `.claude/settings.json`

### 🔴 À faire — bug connu
**"Données acteurs incomplètes" (erreur 400 à la création de garde)**  
L'API `POST /api/gardes` (`app/api/gardes/route.ts`) valide encore `acteurs.nounouPrenom` et `acteurs.famANom`, mais le wizard simplifié ne collecte plus ces champs.  
Fix : supprimer la validation stricte et ajouter des fallbacks (`'Notre nounou'`, `'Famille A'`).

### 🔜 Prochaines priorités

**P1 — Settings : onglet "Personnaliser"**
Ajouter un 4ème onglet dans `app/gardes/[id]/settings/` pour saisir :
- Prénom de la nounou
- Nom affiché Famille A + B
- Prénoms des enfants (remplacer Simone/Giselle/Suzanne)

**P2 — Mode Expert RAC (Étapes 2 & 3 du plan)**
Plan complet dans `~/.claude/plans/generic-beaming-corbato.md` :
- Bouton "⚙️ Ajuster mes aides manuellement" sous les résultats magiques
- Pré-remplissage depuis Mode Magique, réactivité temps réel, bouton Reset
- Fichiers : `lib/calcul.ts` (nouvelles fonctions), `SettingsClient.tsx`, `paie/page.tsx`

**P3 — À investiguer**
- `DATABASE_URL` marqué "Needs Attention" dans Vercel → vérifier la connexion Neon
- Vercel Analytics : vérifier le bon fonctionnement post-migration Clerk

---

## 6. Workflow de Développement

```bash
# QA avant commit (automatisé — voir hook ci-dessous)
npx next lint          # SEUL check bloquant sur Vercel
npx vitest run         # 92 tests métier

# ⚠️ tsc --noEmit échoue localement (types Clerk absents en dev)
# Ne PAS utiliser tsc comme gate de commit — utiliser next lint

# PR workflow
git checkout -b feat/ma-feature
# ... code ...
git push
gh pr create --title "..." --body "..."
gh pr merge <n> --squash
```

### Hook pre-commit ESLint (automatique)
Configuré dans `.claude/settings.json` (gitignored). Avant chaque `git commit`, Claude Code exécute automatiquement `npx next lint`. Si ESLint échoue, le commit est bloqué.

```json
{
  "hooks": {
    "PreToolUse": [{
      "matcher": "Bash",
      "hooks": [{
        "type": "command",
        "command": "cmd=$(jq -r '.tool_input.command'); if echo \"$cmd\" | grep -qE '^git commit'; then cd /Users/arthurboueilh/Documents/GitHub/nannypay-front && npx next lint || exit 1; fi",
        "timeout": 90
      }]
    }]
  }
}
```
Dépendance : `jq` installé via `brew install jq`.

### Règle d'or : poser les questions d'abord
Avant tout coding, poser les questions de clarification jusqu'à 100% de confiance.
Format : Q1 / Q2 / Q3 — réponses courtes attendues — puis go.

### Convention de commits
```
feat: description courte
fix: description courte
# Toujours Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## 7. Points de Vigilance Récurrents

| Problème | Solution |
|----------|----------|
| Clerk v7 vs v5 | Toujours `@clerk/nextjs@^5.7.6` avec Next 14 |
| `NEXT_PUBLIC_*` | Inlinée au BUILD — doit être dans Vercel AVANT le build |
| Input numérique | Pattern `raw` + `focused` (pas de controlled input direct) |
| `repartitionIndemA` ≠ `repartitionA` | Indemnités et salaire se répartissent indépendamment |
| Prisma types | Après migration schema → `npx prisma generate` localement |
| SSG + Clerk | `ClerkProvider` gracieux si clé absente (guard `if (!PUBLISHABLE_KEY)`) |
| `tsc --noEmit` en local | Échoue car `@clerk/nextjs`, `svix` non installés en dev — NE PAS utiliser comme gate CI |
| ESLint `no-unused-vars` | Quand on supprime du JSX, nettoyer **en même temps** les imports, états, fonctions et props qui ne servent plus — sinon Vercel build échoue |
| `next lint` vs `tsc` | Vercel bloque sur ESLint (via `next build`), pas sur les erreurs TypeScript pures — vérifier avec `npx next lint` avant de committer |

---

## 8. Règles Blog et Contenu

### Structure obligatoire de chaque article
Résumé 1 min → intro 2§ → note terminologie 2026 → 5-7 sections H2 numérotées → CTA milieu (après section 3, texte axé démo) → erreurs fréquentes (3-4 items) → CTA final (texte différent, axé onboarding) → sources officielles.

### Résumé 1 min
Principes uniquement — jamais de chiffres précis (ne pas citer 12,89 €/h dans le résumé).

### Exemples
Toujours nommés (Sophie, Lucas, Emma, Théo, Louise…). Jamais « l'employeur » ou « le salarié ».

### Terminologie
Depuis janvier 2026 : « assistant parental » (jamais « garde d'enfants à domicile »). Toujours rappeler en note `.info` en début d'article.

### Chiffres de référence 2026
- Salaire minimum : 12,89 €/h (niveau A), 13,08 €/h (niveau B)
- SMIC : 12,02 €/h
- Heures supplémentaires : +25% de la 41e à la 48e heure, +50% au-delà de 48h
- CMG plafonné : 524 €/mois

### Heures supplémentaires — règles strictes
- Seuil légal : 40h/semaine civile (lundi→dimanche), CCN IDCC 3239 art. 136 et 147
- Deux tranches : +25% (41e→48e h), +50% (>48h). Obligatoires, non supprimables par accord.
- En garde partagée : heures sup générées par une seule famille → à sa charge seule. Si communes → au prorata habituel.
- **INTERDIT** : écrire que les congés payés neutralisent les heures supplémentaires. Les CP couvrent uniquement les heures contractuelles normales. Les heures sup réellement effectuées sont toujours dues en plus.
- Arrêt maladie : la semaine d'arrêt est incomplète — les heures sup ne s'appliquent pas cette semaine-là. Seul le salaire mensualisé est déclaré.

### Absences famille
Les absences d'un enfant (maladie, vacances) ne réduisent pas le salaire total de l'assistant parental. Elles déplacent la charge vers l'autre famille — elles ne la suppriment pas.

### Répartition entre familles
La formule prorata est un point de départ, pas une obligation légale — toujours le préciser. Pour 3 enfants (2 familles, 2+1) : expliquer pourquoi 60/40 et pas 66/33 (plafond CMG non doublé, crédit d'impôt +11% seulement pour un 2e enfant). Mentionner « Mode Magique nounoulink. » pour le calcul automatique du reste à charge.

### Pajemploi — règles de déclaration
- Pajemploi demande le salaire NET (avant impôt à la source), jamais le brut.
- En garde partagée : chaque famille déclare uniquement SA part des heures — jamais le total des deux familles.
- Frais de transport et indemnités de repas : cases dédiées séparées — ne jamais les inclure dans la case salaire net (sinon cotisations inutiles).
- Indemnités de repas : peuvent être regroupées avec les frais de transport dans Pajemploi+.

### CMG
Ne pas développer les détails du CMG dans les articles — sujet complexe et évolutif, risque d'erreur. Se limiter à mentionner son existence et renvoyer vers caf.fr ou le simulateur Urssaf.

### Longueur cible
8-10 min de lecture, 2-4 tableaux de calcul, 1-2 formules mises en avant, 2-4 notes, 1 section erreurs fréquentes.
